#!/usr/bin/env sh

npm run sync:projects || {
  printf '\nCTFramework was committed, but project synchronization FAILED.\nFix the reported problem and rerun npm run sync:projects before releasing consumers.\n' >&2
  exit 1
}
